<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="trackracer.RacerPages.SearchUsersPage"          
             Title="Search Users">

    
    <VerticalStackLayout Padding="20">

       
        <!--<SearchBar x:Name="UserSearchBar"
               Placeholder="Search for users"
               TextChanged="OnSearchTextChanged"
               HorizontalOptions="FillAndExpand" />-->
        <Entry x:Name="RequestText" Placeholder="Request Text" />
        <Entry x:Name="LocationText" Placeholder="Location" />
        <Picker x:Name="TypePicker"
         Title="Select Type"
         ItemsSource="{Binding RequestTypes}"
         SelectedIndexChanged="OnTypeSelected"/>
        <!--<CollectionView ItemsSource="{Binding FilteredItems}" 
                    SelectionMode="Single"
                    SelectionChanged="OnItemSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" BackgroundColor="LightGray" Margin="0,5,0,0">
                        <Label Text="{Binding UserName}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>-->
        <Button Text="Send Track Request" Clicked="Sendtrackrequest" BackgroundColor="#4CAF50" TextColor="White" />
    </VerticalStackLayout>
</ContentPage>
